/*
function ErrorMessage(notice, error, msg, name, EmbedTemplate, AyameClient) {
  const errorEmbed = new EmbedTemplate()
  .setTitle(`Something Went wrong on this command!`)
  .setDescription(`\`${error}\``)
  .addField(`**Report this Command (${name}) in my support server!**`, `${notice}[Support Server](https://discord.gg/h9Tn3MvWSJ)`)
  .setFooter(`Error`, AyameClient.user.displayAvatarURL({ dynamic: true }))
  msg.channel.send(errorEmbed)
}
module.exports = ErrorMessage;
FAILED - (undefined Time) but ill be fixing this and make error handlers for sll cmds :dab:*/
